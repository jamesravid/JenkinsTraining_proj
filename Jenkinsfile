echo "Running Jenkins"
pipeline {
    agent any

    stages {
        stage('Check Node.js Version') {
            steps {
                bat 'node -v' // Outputs the version of Node.js
            }
        }
    }
}
